<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.mapper.MemberMapper">
	
	<!-- 회원가입 -->
	<insert id="join">
	insert into member(userid, userpw, username, email) values(#{userid},#{userpw},#{username},#{email})
	</insert>
	
	<!-- 회원정보 상세조회 -->
	<select id="memberInfo" resultType="com.project.domain.MemberVO">
	select * from member where userid=#{userid}
	</select>
	
	<!-- 회원목록 -->
	<select id="memberList" resultType="com.project.domain.MemberVO">
	select * from member</select>
	
	<!-- 회원정보 수정 -->
	<update id="updateMember">
		update member
			set userpw = #{userpw},
				username = #{username},
				email = #{email}
	    where userid = #{userid};
	</update>
	
	<!-- 회원 삭제 -->
	<delete id="deleteMember">
		delete from member 
		where userid=#{userid};
	</delete>
	
	<!-- 로그인 정보 조회 -->
	<select id="login" resultType="com.project.domain.MemberVO">
		select userid, userpw, username, email from member where userid=#{userid} and userpw=#{userpw}
	</select>
	
	<!-- 아이디 중복체크 -->
	<select id="idChk" parameterType="String" resultType="int">
		select count(*) from member where userid = #{userid}
	</select>
	
</mapper>